const baseUrl = 'http://mobile.icast.co.il/';

const urls = {
  login: 'UserLogin',
  search: 'Search',
  chapters: 'GetChapters',
  chapterInfo: 'GetChapterInfo',
};

const uuid = 'unknown';

const deviceType = 4;

export default {
  deviceType,
  uuid,
  baseUrl,
  urls,
};
